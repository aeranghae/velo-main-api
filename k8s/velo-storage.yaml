apiVersion: v1
kind: PersistentVolume
metadata:
  name: velo-storage-pv
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain  # Pod이 삭제되어도 물리 데이터는 유지하는 정책
  nfs:
    server: 192.168.0.86  # 현재 리눅스 IP
    path: /nfs/velo/storage
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: velo-storage-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
